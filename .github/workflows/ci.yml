name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    name: RunBarCoreChecks
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Run checks
        run: swift run RunBarCoreChecks
